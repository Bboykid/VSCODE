1.
    树的存储结构
2.
    树、森林、二叉树的转化
3.
    树的遍历

    先根遍历
void PreOrder(TreeNode *R)
{
    if(R!=NULL)//树非空
    {
        visit(R);        
        while(R还有未访问的子树)
        {
            PreOrder(T);//先根遍历下一个子树
        }
    }
}

    后根遍历
void PostOrder(TreeNode *R)
{
    if(R!=NULL)//树非空
    {
        while(R还有未访问的子树)
        {
            PostOrder(T);
        }
        visit(R);
    }
}